# Shared configuration contracts

Only configuration contracts that are genuinely shared across deployables belong here. Provider secrets and environment-specific values never belong in this package.

