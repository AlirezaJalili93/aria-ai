# Shared UI

This package is reserved for accessible reusable primitives such as Button, Field, Dialog, Tabs, Toast, EmptyState, LoadingState and RTL layout primitives. Feature-specific UI remains in `apps/web/src/features` until reuse is proven.

