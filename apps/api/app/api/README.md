# API layer

Routers validate transport input, resolve identity and tenant context, invoke application use cases, and map errors. Business logic and direct database/provider access are forbidden here.

